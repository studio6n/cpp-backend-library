#pragma once

#include <cstddef>

namespace impetus {
    namespace shared {
        /**
         * @brief
         * Standard `std::size_t` that is 0.
         */
        constexpr std::size_t K_SIZE_T_NEUTRAL = 0;
    }
}